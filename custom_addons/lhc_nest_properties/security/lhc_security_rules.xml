<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
      Record rules for this addon's models.

      noupdate="0", deliberately. A rule is the security model, not an
      administrator preference: a correction to one has to reach databases that
      already installed the old version. ir.model.access.csv already behaves
      this way — Odoo always reloads it.

      global="True" means the rule is ANDed with every other rule regardless of
      group, so no group membership can widen it. Each model's company scoping
      ships with the model rather than in one project-wide file, so removing an
      addon removes exactly its own rules.
    -->
    <data noupdate="0">
        <record id="rule_lhc_property_company" model="ir.rule">
            <field name="name">Property: multi-company</field>
            <field name="model_id" ref="model_lhc_property"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        </record>

        <record id="rule_lhc_unit_company" model="ir.rule">
            <field name="name">Unit: multi-company</field>
            <field name="model_id" ref="model_lhc_unit"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        </record>

        <record id="rule_lhc_fixture_purchase_company" model="ir.rule">
            <field name="name">Fixture Purchase: multi-company</field>
            <field name="model_id" ref="model_lhc_fixture_purchase"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        </record>

        <record id="rule_lhc_booking_company" model="ir.rule">
            <field name="name">Booking: multi-company</field>
            <field name="model_id" ref="model_lhc_booking"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        </record>
    </data>
</odoo>
